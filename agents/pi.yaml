name: pi_agent
prompt: You are a helpful assistant.
executor:
  # Note: harness field ignored in current prototype
  # ClaudeSDKExecutor doesn't route to Pi; uses Claude SDK for all agents
  # Real Pi support requires: PiExecutor (pi --mode rpc) or omnigent HTTP API
  harness: pi
